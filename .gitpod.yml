image: ghcr.io/tjpalanca/tjcloud/gitpod:latest

tasks:
  - name: Setup Julia Environment 
    init: |
      julia -e ';
        using Pkg
        Pkg.activate("/workspace/AdventOfCode.jl")
        Pkg.instantiate(; verbose = true);
      '

vscode:
  extensions:
    - julialang.language-julia
